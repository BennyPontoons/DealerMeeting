# macOS
.DS_Store

# Photoshop sources — only the exported .jpg files are needed on the web
*.psd

# Claude Code local config
.claude/

# Raw MSRP sheet drop folder (clean copies live in msrp-sheets/)
MSRP SHEET WEBSITE*/
